# Production overrides — usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up
services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    volumes:
      - uploads_data:/app/uploads

  frontend:
    command: npm run start
    volumes: []
