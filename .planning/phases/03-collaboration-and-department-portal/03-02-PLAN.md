---
phase: 03-collaboration-and-department-portal
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/app/(app)/board/_components/SubtaskSection.tsx
  - frontend/src/app/(app)/board/_components/CommentSection.tsx
  - frontend/src/app/(app)/board/_components/TicketDetailModal.tsx
  - frontend/src/app/(app)/board/_components/KanbanCard.tsx
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements:
  - COLLAB-01
  - COLLAB-02
  - COLLAB-03
  - COLLAB-04
  - COLLAB-05
  - COLLAB-06
  - COLLAB-07

must_haves:
  truths:
    - "Ticket detail modal shows a 'Subtasks' section between description and comments with an inline add-input (type and press Enter)"
    - "Subtask items show a checkbox; checking/unchecking persists via PATCH and reflects immediately (optimistic update)"
    - "Subtasks can be dragged to reorder; new order persists via PATCH /reorder"
    - "When all subtasks are checked the badge turns green; no auto-move prompt"
    - "Ticket detail modal shows a 'Comments' section below the activity timeline with a visible text input and submit button"
    - "Comments display in chronological order with author name/avatar"
    - "Delete comment shows a confirm dialog; author or admin can delete; others see no delete button"
    - "Kanban card shows a pill badge with checkmark icon and 'done/total' count (hidden when no subtasks)"
    - "Badge is green when all subtasks are complete"
  artifacts:
    - path: "frontend/src/app/(app)/board/_components/SubtaskSection.tsx"
      provides: "Subtask checklist + inline add + dnd-kit/sortable reorder"
      min_lines: 80
    - path: "frontend/src/app/(app)/board/_components/CommentSection.tsx"
      provides: "Comment thread with always-visible input + delete confirm dialog"
      min_lines: 60
  key_links:
    - from: "frontend/src/app/(app)/board/_components/TicketDetailModal.tsx"
      to: "SubtaskSection, CommentSection"
      via: "JSX import and render between description and activity timeline sections"
      pattern: "SubtaskSection|CommentSection"
    - from: "frontend/src/app/(app)/board/_components/KanbanCard.tsx"
      to: "subtasks_done, subtasks_total fields from board API"
      via: "Conditional pill badge render"
      pattern: "subtasks_done|subtasks_total"
    - from: "SubtaskSection.tsx"
      to: "/api/tickets/{id}/subtasks/reorder"
      via: "fetch PATCH in DnD onDragEnd handler"
      pattern: "subtasks/reorder"
---

<objective>
Build the subtask and comment UI inside the ticket detail modal, and add the subtask count badge to Kanban cards.

Purpose: Makes collaboration features visible to users. Depends on Plan 01 backend endpoints existing.
Output: SubtaskSection.tsx, CommentSection.tsx, both embedded in TicketDetailModal, KanbanCard badge updated.
</objective>

<execution_context>
@/Users/charleskr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/charleskr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-collaboration-and-department-portal/03-RESEARCH.md
@.planning/phases/03-collaboration-and-department-portal/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit/sortable and build SubtaskSection</name>
  <files>
    frontend/package.json
    frontend/package-lock.json
    frontend/src/app/(app)/board/_components/SubtaskSection.tsx
  </files>
  <action>
First install the missing package:
```bash
cd /Users/charleskr/Desktop/XBO/XBO-AI-TeamHub/frontend && npm install @dnd-kit/sortable
```

Then create **frontend/src/app/(app)/board/_components/SubtaskSection.tsx**:

Use TanStack Query to fetch subtasks: `useQuery({ queryKey: ['subtasks', ticketId], queryFn: () => fetch('/api/tickets/${ticketId}/subtasks').then(r => r.json()) })`

**Add subtask input:** A plain `<input>` at the bottom of the list. On Enter keydown: call `useMutation` to POST /api/tickets/{ticketId}/subtasks with { title }, then `queryClient.invalidateQueries(['subtasks', ticketId])`.

**Checklist items:** Each subtask renders a `<Checkbox>` (shadcn/ui) + title span. On check change: `useMutation` PATCH /api/tickets/{ticketId}/subtasks/{id} with { done: !current }. Use optimistic update: `queryClient.setQueryData(['subtasks', ticketId], ...)` then invalidate on settle.

**Drag to reorder:** Use a separate `<DndContext collisionDetection={closestCenter} onDragEnd={handleDragEnd}>` wrapping a `<SortableContext items={subtasks.map(s => s.id)} strategy={verticalListSortingStrategy}>`. Each item uses `useSortable({ id: subtask.id })` from @dnd-kit/sortable. On DragEnd: call `arrayMove` to reorder local state immediately, then fire PATCH /api/tickets/{ticketId}/subtasks/reorder with ordered_ids. This DndContext is NESTED inside the modal but separate from the board's DndContext — no stopPropagation needed, nested DndContext isolation handles it (see RESEARCH.md Pitfall 1).

**Drag handle visual:** A `⋮⋮` span (or GripVertical Lucide icon) with `{...listeners}` — handle-only drag, not the whole row.

**Delete subtask:** Small X button on each item; calls DELETE /api/tickets/{ticketId}/subtasks/{id}; invalidates query.

**Badge color logic:** Count done vs total from the query data; pass counts up via a callback prop `onSubtaskCountChange(done: number, total: number)` so KanbanCard can use board-level data (the modal and card are separate; each manages independently).

Props: `interface SubtaskSectionProps { ticketId: string }`
  </action>
  <verify>
    <automated>cd /Users/charleskr/Desktop/XBO/XBO-AI-TeamHub/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>@dnd-kit/sortable in package.json; SubtaskSection.tsx compiles without TypeScript errors; npm run build passes</done>
</task>

<task type="auto">
  <name>Task 2: CommentSection, embed both in TicketDetailModal, KanbanCard badge</name>
  <files>
    frontend/src/app/(app)/board/_components/CommentSection.tsx
    frontend/src/app/(app)/board/_components/TicketDetailModal.tsx
    frontend/src/app/(app)/board/_components/KanbanCard.tsx
  </files>
  <action>
**frontend/src/app/(app)/board/_components/CommentSection.tsx:**

Use TanStack Query: `useQuery({ queryKey: ['comments', ticketId], queryFn: ... })` fetching GET /api/tickets/{ticketId}/comments.

**Comment display:** Map over comments in chronological order. Each comment shows: author avatar (initials in a colored circle — Claude's discretion per CONTEXT.md), author name, created_at formatted as relative time or date string, body text.

**Comment input:** Always-visible `<textarea>` at the bottom + "Post" button. On submit: POST /api/tickets/{ticketId}/comments with { body }, then `queryClient.invalidateQueries(['comments', ticketId])`. Clear textarea on success. Disable button when textarea is empty.

**Delete button:** Show a trash icon button only when `currentUser.id === comment.author_id || currentUser.role === 'admin'`. On click: open shadcn `AlertDialog` (confirm dialog) — "Delete this comment? This cannot be undone." On confirm: DELETE /api/tickets/{ticketId}/comments/{commentId}, then invalidate.

Use the existing `useAuth` hook or session context for current user. Comments are NOT editable (per CONTEXT.md locked decision).

Props: `interface CommentSectionProps { ticketId: string }`

**frontend/src/app/(app)/board/_components/TicketDetailModal.tsx:**
Add SubtaskSection and CommentSection to the modal body. Section order (per CONTEXT.md locked decisions):
1. Ticket fields / description (existing)
2. **SubtaskSection** — "Subtasks" heading + SubtaskSection component
3. Activity timeline (existing)
4. **CommentSection** — "Comments" heading + CommentSection component

Import both components. Pass `ticketId={ticket.id}` to each.

**frontend/src/app/(app)/board/_components/KanbanCard.tsx:**
The board API endpoint (GET /api/board) must return subtask counts. Update the board endpoint in backend to include subtask counts using a correlated subquery (not selectinload — see RESEARCH.md Pitfall 3). Add to the board query:
```python
# In the board endpoint, add subquery counts to each ticket response
subtasks_total = (select(func.count()).where(TicketSubtask.ticket_id == Ticket.id)).scalar_subquery()
subtasks_done = (select(func.count()).where(TicketSubtask.ticket_id == Ticket.id, TicketSubtask.done == True)).scalar_subquery()
```
Update TicketOut schema to include `subtasks_total: int = 0` and `subtasks_done: int = 0`.

In KanbanCard.tsx, add the badge after the existing card footer badges:
```tsx
{ticket.subtasks_total > 0 && (
  <span className={cn(
    "inline-flex items-center gap-1 text-xs px-1.5 py-0.5 rounded font-medium",
    ticket.subtasks_done === ticket.subtasks_total
      ? "bg-green-100 text-green-700"
      : "bg-slate-100 text-slate-600"
  )}>
    <CheckIcon className="h-3 w-3" />
    {ticket.subtasks_done}/{ticket.subtasks_total}
  </span>
)}
```
  </action>
  <verify>
    <automated>cd /Users/charleskr/Desktop/XBO/XBO-AI-TeamHub/frontend && npm run build 2>&1 | tail -20</automated>
    <manual>Open a ticket detail modal. Verify: Subtasks section appears between description and activity timeline. Add a subtask via Enter key — appears in list with checkbox. Check it — persists on refresh. Drag to reorder — new order persists. Comments section appears below activity timeline. Post a comment — appears with author name. Delete own comment — confirm dialog appears, comment removed.</manual>
  </verify>
  <done>Frontend builds without errors; SubtaskSection and CommentSection present in TicketDetailModal; KanbanCard shows subtask pill badge when subtasks exist</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` passes with no TypeScript errors
2. Open any ticket detail modal — Subtasks section visible between description and timeline
3. Open any ticket detail modal — Comments section visible below timeline with input always shown
4. Add a subtask, check it off, verify Kanban card badge shows "1/1" in green
5. Drag subtask to reorder — confirm new order persists after page refresh
</verification>

<success_criteria>
- SubtaskSection renders in modal with inline add, checkbox toggle, drag reorder
- CommentSection renders in modal with always-visible input, chronological display, author/admin delete with confirm dialog
- KanbanCard shows subtask count pill (hidden when 0, green when all done)
- No DnD interference between subtask list drag and Kanban column drag
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-collaboration-and-department-portal/03-02-SUMMARY.md`
</output>
