---
phase: 02-kanban-core
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Admin and member users can manage the full ticket lifecycle end-to-end"
    - "The complete board-to-detail flow works without errors or visual glitches"
    - "All Phase 2 success criteria are verifiable by a human user"
  artifacts: []
  key_links: []
---

<objective>
Human-verified end-to-end walkthrough of the complete Phase 2 Kanban workflow — confirming that every user-visible behavior works as specified before the phase is marked complete.

Purpose: Phase 2 is the visible heart of the product. Automated tests verify server contracts; this checkpoint verifies the complete user experience.
Output: Confirmed phase completion or a list of issues to fix.
</objective>

<execution_context>
@/Users/charleskr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/charleskr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-kanban-core/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>End-to-end Phase 2 human verification</name>
  <files>none — verification only, no files modified</files>
  <action>
Human walkthrough of the complete Phase 2 Kanban workflow. No code changes — verifies all prior automation worked correctly.

What was built:
- 5-column Kanban board (Backlog, Discovery, In Progress, Review/QA, Done) with dnd-kit drag-and-drop
- Optimistic UI update on drag; server rollback on rejection
- Owner-assignment modal (fires only for unowned Backlog→other moves; cancel snaps card back)
- Quick-add input in Backlog column (title + department required)
- Card metadata: department badge, title, owner initials, due date (red if past), time in column, urgency/priority left border color, effort estimate, next step
- 30-second polling
- Board filter bar with URL persistence (owner, department, priority, urgency, due date range, aging)
- Ticket detail modal at /board?ticket={id} (shareable URL)
- Tiptap rich text editor with 1s auto-save and save-on-blur
- Inline editing of all metadata fields in modal
- Activity timeline (column moves, owner changes)
- Column history section (enter/exit timestamps, time spent)
  </action>
  <verify>
Human walkthrough — see how-to-verify steps below. Resume with "approved" when all 31 steps pass.
  </verify>
  <done>All 31 verification steps pass and user types "approved".</done>
  <how-to-verify>
Start with: `docker compose up` (or confirm already running). Log in as admin@xbo.com. Then verify each item:

**Board Basics**
1. Visit http://localhost:3000/board — confirm 5 columns render with correct names
2. Confirm existing tickets (if any) appear in their correct columns
3. Confirm board refreshes automatically every 30 seconds (watch Network tab — XHR to /api/board)

**Create a Ticket**
4. In the Backlog column quick-add input, type a title (e.g., "Test ticket for Phase 2 verification")
5. Select a department from the dropdown
6. Press Enter or click "+" — ticket appears in Backlog column, owner_id = null
7. Confirm the new card shows the department badge and no owner avatar

**Card Metadata Display**
8. Create 2-3 more tickets with different urgency levels (1, 3, 5) — confirm left border colors: grey (1), blue (3), red (5)
9. Confirm effort estimate shows as "Xh" or "Xd" if set
10. Confirm due date shows in red if date is in the past

**Drag and Drop — Owner Modal**
11. Drag an unowned Backlog ticket to Discovery — owner-assignment modal appears
12. Click Cancel — card snaps back to Backlog (not stuck in Discovery)
13. Drag again → owner modal → select a team member → Confirm — card moves to Discovery, now shows owner initials

**Drag and Drop — Already Owned**
14. Drag the same ticket from Discovery to In Progress — no modal, moves immediately

**Board Filters and URL**
15. Open the filter bar; select a priority filter (e.g., "high") — URL updates to include `?priority=high`; board shows only matching tickets
16. Copy the URL and open it in a new incognito tab (after logging in) — same filter applied
17. Click "Clear all" — URL returns to /board with no filter params; all tickets visible

**Ticket Detail Modal**
18. Click any ticket card — detail modal opens; URL includes `?ticket={id}`
19. Copy URL with `?ticket=...` and paste in new tab — modal opens with that ticket
20. Confirm all fields are visible: title, urgency, priority, department, owner, due date, effort estimate, next step, business impact, success criteria, problem statement editor
21. Edit the title inline — click title, type new text, press Enter — title updates (no save button)
22. Edit priority — select a new value from dropdown — value updates in modal
23. Edit problem_statement in Tiptap editor — type some text — wait 1 second — field auto-saves (no button needed; confirm via Network tab PATCH /api/tickets/{id})
24. Click blur (click outside editor) — save fires immediately
25. Refresh the page — edited fields persist (data was saved to server)

**Activity Timeline**
26. In the detail modal, scroll to Activity section — confirm "created" event is listed
27. Close modal, move the ticket to another column, reopen detail — confirm "moved" event appears with from/to columns

**Column History**
28. In the detail modal, scroll to Column History section — confirm the Backlog entry shows entered_at timestamp
29. After moving, confirm previous column shows exited_at and time spent; new column shows entered_at with no exit

**Close Modal**
30. Click X button or click outside modal — URL reverts to /board (no ?ticket param)

**Admin Delete**
31. In detail modal, find the delete option (admin only) — delete the ticket; confirm it disappears from the board
  </how-to-verify>
  <resume-signal>
Type "approved" if all 31 verification steps pass.

If any steps fail, describe the specific issue (e.g., "Step 12: card stays in Discovery after cancel") — Claude will diagnose and fix, then re-run this checkpoint.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation via resume-signal above.
</verification>

<success_criteria>
All 31 verification steps pass. Phase 2 Kanban Core is complete.
</success_criteria>

<output>
After user types "approved", create `.planning/phases/02-kanban-core/02-04-SUMMARY.md` using the summary template, then update STATE.md to mark Phase 2 complete.
</output>
