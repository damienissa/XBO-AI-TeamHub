---
phase: 04-roi-estimation-and-executive-dashboard
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/app/(app)/board/_components/RoiPanel.tsx
  - frontend/src/app/(app)/board/_components/TicketDetailModal.tsx
  - frontend/src/hooks/useTicketDetail.ts
autonomous: true
requirements: [ROI-04, ROI-05]

must_haves:
  truths:
    - "ROI panel is always visible on the ticket detail — never hidden, never in an accordion"
    - "ROI % is the headline number; annual savings is secondary; supporting grid shows weekly cost, yearly cost, dev cost, adjusted ROI"
    - "When no ROI inputs are filled, panel shows dashes and a subtle 'Add ROI inputs to compute' ghost prompt"
    - "ROI input fields are editable inline with live preview (computed values update as user types)"
    - "Save triggers on field blur — no save button needed"
    - "After blur-save, server-persisted computed values replace local draft (no drift)"
  artifacts:
    - path: "frontend/src/app/(app)/board/_components/RoiPanel.tsx"
      provides: "ROI panel component with inputs, live preview, and hero stats display"
      min_lines: 80
    - path: "frontend/src/app/(app)/board/_components/TicketDetailModal.tsx"
      provides: "Ticket detail modal extended with RoiPanel section"
      contains: "RoiPanel"
  key_links:
    - from: "frontend/src/app/(app)/board/_components/RoiPanel.tsx"
      to: "PATCH /api/tickets/{id}"
      via: "updateMutation.mutate({ roi_field: value }) on input blur"
      pattern: "updateMutation.mutate"
    - from: "frontend/src/app/(app)/board/_components/RoiPanel.tsx"
      to: "ticket.roi / ticket.adjusted_roi"
      via: "TicketOut now includes computed ROI fields from 04-01"
      pattern: "ticket\\.roi|ticket\\.annual_savings"
---

<objective>
Build the RoiPanel component and embed it as an always-visible section in TicketDetailModal, with inline editable ROI inputs and live preview of computed values.

Purpose: Delivers the visible ROI feature to every ticket — the user-facing output of the backend computation built in Plan 01.
Output: RoiPanel.tsx component, TicketDetailModal extended with ROI section.
</objective>

<execution_context>
@/Users/charleskr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/charleskr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-roi-estimation-and-executive-dashboard/04-CONTEXT.md
@.planning/phases/04-roi-estimation-and-executive-dashboard/04-RESEARCH.md
@.planning/phases/04-roi-estimation-and-executive-dashboard/04-01-SUMMARY.md
@frontend/src/app/(app)/board/_components/TicketDetailModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoiPanel component</name>
  <files>
    frontend/src/app/(app)/board/_components/RoiPanel.tsx
  </files>
  <action>
Create `frontend/src/app/(app)/board/_components/RoiPanel.tsx` as a client component (`"use client"`).

Props: `ticket: TicketOut` (the full ticket object from TanStack Query), `onUpdate: (fields: Partial<TicketOut>) => void` (calls updateMutation.mutate in the parent).

**Local state for live preview:**
```typescript
const [draft, setDraft] = useState({
  current_time_cost_hours_per_week: ticket.current_time_cost_hours_per_week ?? null,
  employees_affected: ticket.employees_affected ?? null,
  avg_hourly_cost: ticket.avg_hourly_cost ?? null,
  expected_savings_rate: ticket.expected_savings_rate ?? null,
  risk_probability: ticket.risk_probability ?? null,
  strategic_value: ticket.strategic_value ?? null,
});
```

**Live computed preview** (mirrors server formula, for display before blur-save):
- `liveWeeklyCost = draft.current_time_cost_hours_per_week * draft.employees_affected * draft.avg_hourly_cost` (null if any is null)
- `liveYearlyCost = liveWeeklyCost * 52`
- `liveAnnualSavings = liveYearlyCost * draft.expected_savings_rate`
- Display `ticket.roi` and `ticket.adjusted_roi` from server (do not recompute ROI/adjusted_roi in browser — these require effort_estimate which is outside this component)

**Blur handler:** `const handleBlur = (field: string, value: number | null) => { onUpdate({ [field]: value }); }` — this triggers PATCH via the parent's updateMutation.

**Ghost state:** `const hasAnyRoiInput = !!(draft.current_time_cost_hours_per_week || draft.employees_affected || draft.avg_hourly_cost);`

**Layout (CONTEXT.md spec):**
- Hero row: ROI % large (from `ticket.roi`, formatted as `(v * 100).toFixed(1)%` or "—") + Annual Savings large (from `ticket.annual_savings`, formatted as `$N` or "—")
- Supporting 4-cell grid below hero: Weekly Cost | Yearly Cost | Dev Cost | Adjusted ROI
- When `!hasAnyRoiInput`: show a subtle muted text below the hero: "Add ROI inputs below to compute"
- Input section header: "ROI Inputs"
- Row 1 inputs (horizontal flex): Hours Saved/Week, Employees Affected, Avg Hourly Cost ($) — all `type="number"` inputs, `min="0"`, value from draft, onChange updates draft (live preview), onBlur calls handleBlur
- Row 2 inputs: Expected Savings Rate (0-1), Risk Probability (0-1), Strategic Value (1-5 integer)
- Collapsible section "Optional Inputs" containing: Current Error Rate, Revenue Blocked — use a simple `<details>` or a controlled `useState(false)` toggle with a chevron button

**Formatting helpers:**
```typescript
const fmt$ = (v: number | null) => v == null ? "—" : `$${v.toLocaleString("en-US", { maximumFractionDigits: 0 })}`;
const fmtPct = (v: number | null) => v == null ? "—" : `${(v * 100).toFixed(1)}%`;
```

Style with Tailwind. Use existing card/section visual language from the modal (border, rounded, padding). Input style: match the inline edit inputs already in TicketDetailModal (border-input, focus:ring, h-8 or similar).

**After onUpdate success:** The parent re-fetches via invalidateQueries, updating `ticket` prop. Reset draft from the new `ticket` prop values in a `useEffect([ticket])` to prevent drift (RESEARCH.md Pitfall 6).
  </action>
  <verify>
    <automated>cd /Users/charleskr/Desktop/XBO/XBO-AI-TeamHub/frontend && npx tsc --noEmit 2>&1 | grep "RoiPanel" | head -20</automated>
    <manual>No TypeScript errors in RoiPanel.tsx. Component file exists at the correct path.</manual>
  </verify>
  <done>RoiPanel.tsx compiles clean; exports default RoiPanel component; has hero stats, supporting grid, two input rows, collapsible optional inputs, blur-to-save, live preview, ghost state.</done>
</task>

<task type="auto">
  <name>Task 2: Embed RoiPanel in TicketDetailModal</name>
  <files>
    frontend/src/app/(app)/board/_components/TicketDetailModal.tsx
    frontend/src/hooks/useTicketDetail.ts
  </files>
  <action>
Open `frontend/src/app/(app)/board/_components/TicketDetailModal.tsx`.

1. Import `RoiPanel` from `./RoiPanel`.

2. Add `RoiPanel` as an always-visible section between the subtask section and the comment section (or at the bottom of the modal body — after comments, before the timeline). It must NOT be inside an accordion or conditional render. Use the same visual section wrapper (e.g., a `<div className="border rounded-lg p-4">` or whatever pattern the modal uses for SubtaskSection/CommentSection).

3. Pass props: `<RoiPanel ticket={ticket} onUpdate={(fields) => updateMutation.mutate(fields)} />`

4. Verify the `updateMutation` already accepts partial ticket fields (it should — it calls `PATCH /api/tickets/{id}` with the supplied body). If `updateMutation.mutate` expects a specific type, ensure the type accepts the new ROI fields. The mutation is defined in `useTicketDetail.ts` — check that its `mutationFn` passes the body through without filtering, or update it to pass all fields from the update object.

5. If `TicketOut` type (in `src/types/ticket.ts` or wherever it's defined) is missing the new ROI fields, add them as optional: `current_time_cost_hours_per_week?: number | null`, etc. for all 14 new columns. This is likely auto-generated or manually defined — update it to match the new TicketUpdate/TicketOut schema.

Do not change any existing modal layout, tab, or accordion behavior — only add the RoiPanel section.
  </action>
  <verify>
    <automated>cd /Users/charleskr/Desktop/XBO/XBO-AI-TeamHub/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Zero TypeScript errors across the project. Open a ticket detail in the browser — ROI panel is visible as a section in the modal, below/after the existing sections.</manual>
  </verify>
  <done>RoiPanel renders inside TicketDetailModal with no TypeScript errors; panel is always-visible; onUpdate calls PATCH; TicketOut type includes all new ROI fields.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. RoiPanel.tsx exists at `frontend/src/app/(app)/board/_components/RoiPanel.tsx`
3. `grep -n "RoiPanel" frontend/src/app/(app)/board/_components/TicketDetailModal.tsx` shows import and usage
4. `grep -n "current_time_cost_hours_per_week" frontend/src/types/ticket.ts` (or equivalent type file) shows field present
</verification>

<success_criteria>
- RoiPanel is always visible on ticket detail — never hidden
- ROI hero stats display from server-persisted values (or "—" when null)
- Inline inputs update draft state for live preview; blur triggers PATCH
- After successful PATCH, draft resets from updated ticket prop (no drift)
- Zero TypeScript compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-roi-estimation-and-executive-dashboard/04-02-SUMMARY.md`
</output>
